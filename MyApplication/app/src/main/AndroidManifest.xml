<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <!-- Update the SplashActivity intent-filter to make it the launcher activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"> <!-- Make sure this is true -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Remove the MAIN action and LAUNCHER category from MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- The rest of your activities -->
        <activity
            android:name=".SignupActivity"
            android:exported="true" /> <!-- This should be exported="true" to be accessible -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" /> <!-- This should be exported="true" to be accessible -->

    </application>

</manifest>
